---
name: humanizer-skill-creator
description: 사용자의 글 샘플에서 문체 프로필과 평가셋을 만든 뒤, 그 사람 전용 퇴고 스킬을 작성한다. 동료가 내 말투 스킬, 문체 스킬, 필력, 나처럼 글 쓰는 스킬, 미감 있는 글, 우리 팀 톤, humanizer skill 만들기를 요청할 때 사용한다. 이미 있는 글을 지금 고치라는 요청에는 쓰지 않는다 — 그건 그 사람이 이미 가진 퇴고 스킬의 일이다.
argument-hint: "[모드: create / eval / improve] [샘플: 본인 글 붙여넣기] [대상 폴더: 생략 시 확인]"
metadata:
  version: "2.0"
---

# humanizer-skill-creator — 그 사람 전용 퇴고 스킬 만들기

남의 글을 “사람답게” 만드는 범용 humanizer가 아니다. **이 사람의 글에서 AI 티만 걷고, 이 사람이 일부러 쓰는 말은 남기는** 퇴고 스킬을 만드는 절차다.

동료 각자가 자기 말투 스킬을 갖는 것이 목표다. 사람이 읽을 안내는 [README.md](README.md). 커맨드로 들어왔을 때의 진입 규칙은 [commands/말투.md](commands/말투.md), 평가 실행은 [commands/말투-평가.md](commands/말투-평가.md).

이 저장소에는 완성된 개인 스킬을 두지 않는다. 산출은 각자 개인 폴더에 생긴다 — 남의 말투를 정본처럼 섞지 않기 위해서다.

성공 create의 Input/Output: [examples.md](examples.md). 대조 절차: [references/contrast-mining.md](references/contrast-mining.md). 시드(필터 재료): [references/seed-tells.md](references/seed-tells.md). 평가셋 4축: [references/eval-protocol.md](references/eval-protocol.md). eval을 실제로 돌리는 법: [eval/RUN.md](eval/RUN.md). 생성 스킬 골격: [references/skill-anatomy.md](references/skill-anatomy.md). 설계 출처: [references/community-lessons.md](references/community-lessons.md).

## 이 스킬이 아닌 것

- 글을 지금 고치는 일 → 그 사람이 이미 가진 퇴고 스킬을 쓴다.
- 범용 “AI 냄새 50개 목록” 스킬을 한 번에 심는 일 → 만들지 않는다.
- 화면 미감(색·폰트·레이아웃) → 여기 미감은 **문장 호흡·단락·구두점·만질 수 있는 비유**다.
- 탐지기 점수 올리기·우회 → 범위 밖.
- 저장소·outbox·메일함에서 “그럴듯한 본인 글”을 골라 샘플로 삼는 일 → 저자가 낸 글만 쓴다.

## 핵심 원칙

1. **평가셋이 스킬보다 먼저다.** 실패하는 시험 없이 SKILL.md를 확정하지 않는다. 얇은 초안은 한 편 시험과 같이 쓰고, 평가셋 4개 없이 v1을 선언하지 않는다.
2. **냄새와 함정을 가른다.** 같은 표면형도 문맥에 따라 보존일 수 있다. 저자가 확정한 보존 표현은 함정이다. 잡으면 즉시 실패.
3. **금칙어 목록을 본체로 두지 않는다.** 구조·남용·문맥이 본체이고, 단어는 신호일 뿐이다.
4. **실제 글이 이기고, 추구 문체는 확인받는다.** “김훈처럼”이 평소와 충돌하면 묻고, 답 없이 씌우지 않는다.
5. **지어내지 않는다.** 사람 냄새를 낸다고 사실·숫자·인용·고유명사를 보태지 않는다.
6. **샘플은 저자가 낸다.** 경로를 사용자가 집지 않았으면 저장소를 뒤져 고르지 않는다. 붙여 넣거나 이 창에 직접 쓰게 한다.
7. **한 턴에 질문 하나.** 쓰임새·글·함정·시험 초안을 한 번에 묻지 않는다. 답이 오기 전에 다음으로 가지 않는다.
8. **eval은 모델을 돌려야 eval이다.** 채점기 출력만으로 통과를 주장하지 않는다. 자세히는 [eval/RUN.md](eval/RUN.md).

## 산출물 (create가 끝나면 이 폴더가 있다)

```
{skill-name}/
├── SKILL.md                 # 퇴고 절차. 체크리스트가 정본
├── STYLE-PROFILE.md         # 관찰된 문체 사실 (형용사 인상 아님)
├── eval/
│   ├── AUTHOR-TRAPS.md      # 저자 확정 보존 목록
│   ├── README.md
│   ├── judge-rubric.md
│   ├── grade.py
│   └── cases/{NN-슬러그}/
└── eval/results/
```

골격은 [templates/](templates/)를 복사한 뒤 채운다.

**저장 위치.** 기본은 `~/.claude/skills/{name}/`이다. 프로젝트 안에서만 쓸 스킬이면 그 프로젝트의 `.claude/skills/{name}/`. **쓰기 전에 한 번 확인한다.** 이 저장소 안에 만들지 않는다. 남의 스킬을 덮어쓰지 않는다.

이름은 소문자·숫자·하이픈, 저자나 장르가 보이게(`minji-blog-voice`). `humanizer` 단독은 범용과 충돌한다.

## 실행 모드

인자에 따라 시퀀스 중간으로 점프한다. 불명하면 create다.

| 모드 | 언제 |
|---|---|
| **create** | 새 개인 퇴고 스킬이 필요할 때. 아래 체크리스트 |
| **eval** | 이미 스킬이 있을 때 평가셋만 다시 돌릴 때 |
| **improve** | 실전에서 놓친 냄새·오탐이 생겼을 때. 실패 케이스부터 추가 |

한 턴에 폴더만 만들고 끝내지 않는다. 쓰임새가 정해지고 그 맥락의 글 1편이 있으면 1–2까지 간다. 평가셋으로 개선하는 게 본선이다.

---

## create 체크리스트

응답에 복사하고 칸을 친다. 앞 단계 산출 없이 뒤 단계로 가지 않는다.

```
Create:
- [ ] 0a 어디에 쓸지 — 아래 옵션으로 하나만 묻는다. 답이 오면 다음
- [ ] 0b 그 맥락에서 쓴 글 1편(대략 200자+) — 저자가 붙이거나 지금 쓴다. 에이전트가 고르면 정지
- [ ] 1 문체 프로필 — 관찰만. 함정은 한 줄씩, 최대 3개까지만 묻는다
- [ ] 2 얇은 초안 + 저자가 고치고 싶은 초안 1편 시험 (시험 초안도 따로 한 번 묻는다)
- [ ] 3 평가셋 4개를 만들고 실제로 돌려 개선. 4개 없이 v1 금지
- [ ] 4 description 트리거 점검 — 이 저자가 실제로 쓸 말로 켜지는가
```

---

## create 시퀀스

뒤 단계는 앞 단계 산출 위에서만 돈다. 게이트에서 멈추면 억지로 스킬을 쓰지 않는다.

### 0a. 어디에 쓸지 (첫 질문, 이것만)

create가 시작됐는데 쓰임새가 아직 없으면 **이 질문만** 하고 멈춘다. 글·호출어·함정을 같이 묻지 않는다. 저장소를 뒤지지 않는다.

기본 옵션 (번호로 고르게 한다. 다른 쓰임이면 그 한 줄을 쓰게 한다):

1. 블로그·에세이·SNS
2. 슬랙·디스코드 공지, 운영 안내
3. 메일 (외부 또는 팀)
4. 강의·미션·LMS 안내
5. 보고서·위키·업무 문서
6. 책·긴 원고
7. 유튜브·대본
8. 기타 (한 줄로)

고른 값이 장르·문체 잠금과 호출어의 재료가 된다. 호출어는 따로 묻지 말고, 쓰임새에서 만든다 (예: 2 → “공지 말투”, “운영 안내처럼”).

### 0b. 그 맥락의 글 1편 (두 번째 질문, 이것만)

쓰임새가 정해진 뒤에만 묻는다. 다른 질문은 붙이지 않는다.

```
방금 고른 {쓰임새}에서, 본인이 쓴 글 1편을 붙여 넣거나 이 창에 지금 써 주세요.
대략 200자면 됩니다. 인사말·한 줄은 안 됩니다.
```

“알아서 골라”, “내 글 많잖아”여도 고르지 않는다. 사용자가 **경로를 집어서** 읽어 달라고 한 파일만 읽는다.

**1–2를 시작하려면** 0a와 0b가 둘 다 있어야 한다. 에이전트가 찾은 글은 해당 없음.

키워드·인사말·문장 두 개면 그 맥락의 글을 다시 달라고만 한다. 빈 골격을 부풀리지 않는다.

**추구 문체**는 아직 묻지 않는다. 프로필을 보여 준 뒤에, 평소와 충돌할 때만 한 줄로 묻는다.

### 1. 대조 → STYLE-PROFILE.md

절차는 [references/contrast-mining.md](references/contrast-mining.md). 시드 이름만 필요할 때 [references/seed-tells.md](references/seed-tells.md)를 읽는다. 시드는 **필터 재료**다. 샘플과 대조에서 살아남은 유형만 체크리스트에 올린다. 위키백과·공개 패턴을 통째로 넣지 않는다.

결과는 인상이 아니라 **관찰**이다.

- 호흡 (문장 길이의 들쭉날쭉, 접속, 단락 첫 문장)
- 구두점·강조 버릇
- 반복되는 입버릇과 좋아하는 구체 비유
- AI 초안이 손대려 하고 본인은 남기는 것 → **함정 후보**
- 본인이 고친 것, 또는 샘플에 거의 안 나오는데 AI 초안에만 있는 것 → **냄새 후보**

추구 문체가 평소 글과 다르면 충돌 표를 만들고 저자에게 확인한다. 확인 전엔 평소 글을 기본값으로 둔다.

#### 함정 확인 — 세 번까지만 묻는다

함정 후보는 **한 줄만** 보여 주고 `보존 / 줄임 / 모르겠다`를 받는다. 답이 오면 다음 함정.

- **상한 3개.** 세 번째 답을 받으면 더 묻지 않고 2로 간다. 나머지 후보는 STYLE-PROFILE 「미확정」에 적어 두고, 실전 오탐이 나올 때 improve로 올린다.
- **「모르겠다」는 보류다.** 냄새로 지우지 않고, 함정으로 확정하지도 않는다. AUTHOR-TRAPS에 `보류`로 적는다. 같은 질문을 다시 묻지 않는다.
- **「모르겠다」가 두 번 연속이면** 질문을 멈추고 2로 간다. 판단 재료가 아직 없다는 뜻이다 — 한 편 시험을 보면 저자가 답할 수 있게 된다.
- 저자가 “그만”, “넘어가자”라고 하면 즉시 2로 간다.
- 확신이 필요한 순서로 묻는다: 저자 글에 **반복되는 것** → 오프너·종결 습관 → 어휘 하나. 어휘 하나만 묻고 끝내지 않는다.

한 번에 세 개를 고르게 하지 않는다. 확인 전에 체크리스트를 확정하지 않는다.

체크리스트 한 줄을 자기 말로 고치는 요청은 함정 확인 다음에, 그것만 따로 한다.

### 2. 얇은 초안 + 저자의 초안 1편 시험

[templates/SKILL.md](templates/SKILL.md)를 복사하되, 지금은 체크리스트 2~4행만 채운다. 행은 1에서 살아남은 것만. 필수 절(적용/보류, 가드레일, 출력, 재통독)은 뼈대만 남긴다.

시험 재료는 저자가 낸, 지금 고치고 싶은 초안이다. 아직 없으면 **그것만** 달라고 하고 만든다. 받은 뒤에 before/after와 변경 기록을 보여 준다. 글 1편(0b)과 시험 초안을 한 턴에 같이 받지 않는다.

이 한 편이 첫 평가다. 저자가 “이건 내 말 아니다”라고 하면 초안을 고치고 같은 편을 다시 돌린다. 폴더 골격과 `grade.py`는 아직 완성본이 아니다.

### 3. 평가셋으로 개선 — 이 전에는 v1을 선언하지 않는다

한 편 시험 뒤에 평가셋 4개를 만들고, 틀린 축 하나만 고쳐 다시 돈다. 글 3편을 미리 받지 않는다. 더 필요하면 그 맥락의 글을 **한 편씩** 달라고 한다.

[references/eval-protocol.md](references/eval-protocol.md)와 [templates/eval/](templates/eval/)을 따른다. 최소 구성:

| 케이스 | 축 | 역할 |
|---|---|---|
| 01~ | 탐지 | 심어 둔 냄새가 수정본에서 사라지는가 |
| 0n | 분별 | 함정이 원문 그대로 남는가 (훼손 = 즉시 FAIL) |
| 클린대조군 | 분별 | 이미 사람 글인 샘플을 과잉 적용하지 않는가 |
| 경계 | 규율 | 애매하면 보류인가. 적용하면 FAIL |

라벨 3종: `planted` / `traps` / `known_borderline`. 함정은 가능하면 저자가 실제로 보존한 문장에서 가져온다.

본문은 평가용으로 새로 써도 된다. 저작권 있는 원문을 몰래 넣지 않는다.

**돌리는 법은 [eval/RUN.md](eval/RUN.md)다.** 케이스를 쓴 에이전트가 답도 쓰면 그건 채점이 아니다. 깨끗한 문맥에서 실행하고, `grade.py`로 채점하고, 규율·품질은 사람/판정 루브릭이 본다.

합격선 기본값: planted 회수율 ≥ 0.66, 함정 훼손 0, 클린대조군 적용 ≤ 3, 규율 위반 0. 적용 모드에서는 오탐이 본문 훼손이므로 정밀도를 우선한다.

평가셋 4개가 파일로 있고, **실제 실행 결과가 `eval/results/`에 있고**, 2의 한 편 시험이 저자 확인을 지났을 때 v1이다.

### 4. description 트리거 점검

description에는 하는 일과 켜지는 상황을 함께, 3인칭으로 적는다. 다른 사람 프로젝트에 이 문체를 강제하는 문장은 넣지 않는다.

그다음 **저자가 실제로 쓸 문장 3개**를 저자에게 받거나 저자 말투로 적어 보고, 그 description이 켜지는지 본다. 안 켜질 것 같으면 트리거 어휘를 그 사람 말로 바꾼다("퇴고" 대신 "다듬어", "말투 고쳐"). 반대로 남의 장르 요청에도 켜질 것 같으면 한계 문장을 넣는다.

### 5. 더 깎을 때 (선택)

스킬 없이 “AI 티를 사람 글로 고치라”만 준 baseline이 이미 통과하는 케이스는 버린다. 나머지를 with-skill과 나란히 돌린다.

| 신호 | 손볼 곳 |
|---|---|
| planted 회수 낮음 | 체크리스트에 그 유형·짧은 예시를 추가. 금칙어를 늘리지 말고 유형을 선명히 |
| 함정 훼손 | 가드레일·AUTHOR-TRAPS·“같은 표면형, 다른 문맥” 예시를 추가 |
| 회차마다 판정이 갈림 | 조건문을 더 구체로 |
| 클린대조군 적용 과다 | 적용 문턱을 높이고 보류를 기본으로 |
| after가 새 냄새 | 재통독을 출력 전 필수로 |

고치기 전에 실패하는 케이스가 파일로 있어야 한다. 고친 뒤에는 그 케이스와 회귀를 다시 돈다.

---

## 작성 스킬을 원할 때

기본 산출은 **퇴고 스킬**이다. 빈 칸에서 그 사람처럼 *쓰게* 하는 스킬은 다른 일이다.

작성까지 요청하면: 먼저 **문체 잠금을 깨는 초안 1개**를 평가셋에 넣는다 (실패가 템플릿보다 먼저다). 그다음 생성된 SKILL.md에 “작성 시 문체 잠금 + 정본 문장 2~3개만 본다” 절을 더한다. “영혼을 담아”, “더 살아 있게” 같은 형용사는 넣지 않는다. 시각 미감 규칙은 넣지 않는다.

## eval 모드

기존 개인 퇴고 스킬 폴더를 받아 [eval/RUN.md](eval/RUN.md)대로 baseline 또는 with-skill을 돌린다. 스킬 문구를 고치고 싶다는 요청이 없으면 결과와 판정만 남긴다.

반복 3회가 기본이다. 인프라 점검만이면 1회. 분산이 크면 그 자체를 보고한다.

## improve 모드

실전 오탐·미탐을 케이스 한 개로 만든 다음 스킬을 고친다.

1. before 문장, 저자 판정(적용했어야 / 보류 / 건드리면 안 됨), 이유를 받는다.
2. `cases/{NN}/`를 추가하고 baseline이 실패하는지 확인한다.
3. 그 실패를 겨냥해 체크리스트 또는 가드레일만 최소로 고친다.
4. 신규 + 회귀를 돈다.

저자가 “이건 내 말이야”라고 한 표현은 AUTHOR-TRAPS에 `확정`으로 올리고, expected.json 함정에 승격한다. 1에서 `보류`로 남은 줄이 여기서 확정되는 것이 정상 경로다.

## 출력 (이 메타 스킬의 보고)

한 줄 + 경로 + 아직 안 한 것.

```
[/humanizer-skill-creator create {name}] 샘플 N편 대조, 냄새 유형 n, 함정 m (보류 k).
평가셋 j케이스 (클린 1 포함). 실행: {조건} {회차}회 → PASS/FAIL.
파일: {폴더}/SKILL.md, {폴더}/eval/
아직 안 함: {baseline 미실행 / 반복 1회 / 보류 항목 k개}
```

중간에 멈췄으면 왜 멈췄는지와 다음에 필요한 입력을 적는다. 다른 스킬을 자동으로 켜라고 권하지 않는다.

## 흔한 실수

- 샘플 없이 위키백과·공개 목록을 이식한다 → 필터 없는 목록은 스킬이 아니다.
- 저장소에서 “본인 글 같으면” 골라 파일럿을 시작한다 → 저자가 내거나 경로를 집기 전에는 정지.
- 쓰임새·글·호출어·함정을 한 번에 묻는다 → 한 턴에 질문 하나. 첫 질문은 어디에 쓸지.
- 함정을 다섯 번, 여섯 번 계속 묻는다 → 상한 3개. 「모르겠다」 두 번이면 시험으로 넘어간다.
- 평가셋 없이 SKILL.md를 확정한다 → 한 편 시험은 초안용이고, 4개 없이 v1이 아니다.
- **케이스를 쓴 에이전트가 정답 파일까지 써서 `grade.py`를 돌리고 통과라고 한다** → 자기 답을 자기가 채점한 것이다. [eval/RUN.md](eval/RUN.md).
- 평소 글의 대시·짧은 문장·입버릇을 AI 티로 지운다 → 그건 목소리다. 함정.
- 추구 문체를 확인 없이 씌운다 → 다른 사람 흉내 스킬이 된다.
- 적용을 기본으로 두고 경계까지 고친다 → 오탐이 본문을 훼손한다.
- 사람 냄새를 위해 구체적인 사실을 보탠다 → 금지.
- 남의 스킬 폴더를 덮어쓴다 → 새 이름, 새 버전 라벨.

## 검증

이 메타 스킬을 고치면 [eval/](eval/README.md)의 압력 시나리오를 다시 본다. 생성된 스킬을 고치면 그 스킬의 `eval/`을 돈다.
